query reactForceData {
  reactForceData{
    nodes{
      id
      label
      desc
      nodeVal
      color
      autoColorBy
      group
    }
    links{
      source
      target
      label
      desc
      color
      autoColorBy
      group
    }
  }
}
